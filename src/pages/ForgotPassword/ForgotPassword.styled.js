import styled from 'styled-components';
import { SaveButton } from '../../components/SettingModal/SettingModal.styled';

export const StyledButton = styled(SaveButton)`
  width: ${(props) => props.$width};
  margin: 0;
`;
